<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background -->
    <solid android:color="#FFF8FA"/>

    <!-- Corner Radius -->
    <corners android:radius="18dp"/>

    <!-- Border -->
    <stroke
        android:width="1dp"
        android:color="#F1D5E1"/>

    <!-- Padding -->
    <padding
        android:left="16dp"
        android:top="12dp"
        android:right="16dp"
        android:bottom="12dp"/>

</shape>